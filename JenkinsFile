pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                url: 'https://github.com/mehdi147899/deops_projet.git',
                branch: 'BaghouliAmine',
                credentialsId: 'github'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                sh 'mvn sonar:sonar -Dsonar.projectKey=sonardevops -Dsonar.sources=src/main/java -Dsonar.host.url=http://192.168.1.101:9000 -Dsonar.token=squ_0cde13cc506c9f2f7da5b140a90e8ba33f9289bf '
            }
        }
        stage('Quality Gate') {
            steps {
                timeout(time: 15, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }
    }
}
